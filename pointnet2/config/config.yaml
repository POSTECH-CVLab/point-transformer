defaults:
    - task: sem
    - model: ssg
    - task_model: ${defaults.0.task}-${defaults.1.model}

hydra:
  run:
    dir: outputs

gpus:
    - 2

optimizer: ???

task_model: ???

model: ???

distrib_backend: dp
